rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Anyone can view uploaded assets
      allow read: if true;
      // Only authenticated admins can upload or delete assets
      allow write: if request.auth != null;
    }
  }
}
