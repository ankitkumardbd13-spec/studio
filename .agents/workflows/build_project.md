---
description: Build the complete project
---
# Build Project

This workflow installs project dependencies and builds the application for production.

1. **Install Dependencies**
   Run `npx pnpm install` to install all necessary packages.

2. **Build the Application**
   Run `npx pnpm run build` to compile the Next.js application.
