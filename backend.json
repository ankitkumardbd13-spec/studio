{
  "collections": {
    "students": {
      "description": "Student profiles and course enrollment details.",
      "documents": {
        "studentId": {
          "name": "string",
          "fatherName": "string",
          "rollNo": "string",
          "trade": "string",
          "year": "number",
          "session": "string",
          "aadhaar": "string",
          "mobile": "string",
          "dob": "string",
          "photo": "string",
          "status": "string (pending/approved)"
        }
      }
    },
    "assignments": {
      "description": "MCQ tests generated by Admin.",
      "documents": {
        "assignmentId": {
          "title": "string",
          "trade": "string",
          "year": "number",
          "subject": "string",
          "durationMinutes": "number",
          "questions": "array",
          "deadline": "timestamp",
          "createdAt": "timestamp"
        }
      }
    },
    "results": {
      "description": "Student test performance records.",
      "documents": {
        "resultId": {
          "studentId": "string",
          "assignmentId": "string",
          "score": "number",
          "total": "number",
          "percentage": "number",
          "status": "string (Pass/Fail)",
          "timestamp": "timestamp"
        }
      }
    },
    "fees": {
      "description": "Payment history and balance details.",
      "documents": {
        "feeId": {
          "studentId": "string",
          "amount": "number",
          "date": "string",
          "mode": "string",
          "particulars": "string",
          "receiptNo": "string"
        }
      }
    },
    "siteSettings": {
      "description": "Global configuration for logos, stamps, and messages.",
      "documents": {
        "config": {
          "logo": "string",
          "stamp": "string",
          "address": "string",
          "heroTitle": "string",
          "heroSub": "string"
        }
      }
    }
  }
}
